# sim-prep-automated
project to automatically prepare simulators for use on a periodic timer

User service should live here:
~/.config/systemd/user/sim-prep.service

The service runs /home/joby/sim_prep/sim-prep.sh to prepare the sim

